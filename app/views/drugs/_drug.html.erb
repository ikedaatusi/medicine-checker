<% content_for(:title) %>
<div class="col-sm-12 col-lg-4 mb-3">
  <div id="drug-id-<%= drug.id %>">
    <div class="card">
    <% @drugs.each do |t| %>
    <%= image_tag t.image_url_url, class: 'card-img-top', size: '300x200' %> 
      <div class="card-body">
        <ul class="list-inline">
          <li><%= t.hospital_name %></li>
          <li><%= t.drug_name %></li>
          <li><%= t.number_of_tablets %></li>
          <!-- 複数服用タイミングを表示させる　-->
          <% t.take_times.each do |take_time| %>
          <li><%= take_time.take_time %></li> 
          <% end %>
          <li><%= l t.created_at, format: :short %></li>
        </ul>
    <% end %>
        <h4 class="card-title">
            <%= link_to "#" %>
          </h4>
      </div>
    </div>
  </div>
</div>