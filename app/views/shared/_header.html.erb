<nav class="bg-white border-gray-200 dark:bg-gray-900">
    <div class="flex flex-wrap items-center justify-between max-w-screen mx-auto p-4">
        <a href="/" class="flex items-center space-x-3 rtl:space-x-reverse">
            <%= image_tag 'medicine-checker.webp', size: "70x70" %>
            <span id="mega-menu-icons" class="self-center text-2xl font-semibold whitespace-nowrap hidden md:block dark:text-white">medicinechecker</span>
        </a>
      <div class="grid items-center md:order-2 space-x-1 md:space-x-2 rtl:space-x-reverse">
            <%= link_to "ログアウト", logout_path, class: "text-white hidden md:block bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 md:px-5 md:py-2.5 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800", data: { turbo_method: :delete } %>
        <nav>
          <button id="button" type="button" class="fixed top-6 right-6 z-10 ">
            <i id="bars" class="fa-solid fa-bars fa-2x text-white"></i>
            <i id="xmark" class="fa-solid fa-xmark fa-2x hidden text-white"></i>
          </button>
          <ul id="menu" class="fixed top-0 left-0 z-0 w-full translate-x-full bg-blue-500 text-center text-xl font-bold text-white transition-all ease-linear">
            <li class="p-3"><a href="">TOP</a></li>
            <li class="p-3"><a href="">BLOG</a></li>
            <li class="p-3"><a href="">CONTACT</a></li>
          </ul>
        </nav>
        <main>
    <script>
      button.addEventListener('click', event => {
        bars.classList.toggle('hidden')
        xmark.classList.toggle('hidden')
        menu.classList.toggle('translate-x-full')
      });
    </script>
  </main>
      </div>
    </div>
    <button class="btn" onclick="my_modal_1.showModal()">open modal</button>
<dialog id="my_modal_1" class="modal">
  <div class="modal-box">
    <h3 class="font-bold text-lg">Hello!</h3>
    <p class="py-4">Press ESC key or click the button below to close</p>
    <div class="modal-action">
      <form method="dialog">
        <!-- if there is a button in form, it will close the modal -->
        <button class="btn">Close</button>
      </form>
    </div>
  </div>
</dialog>
</nav>

